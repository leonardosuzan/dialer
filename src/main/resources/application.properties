spring.batch.initializer.enabled=true
spring.batch.job.enabled=false

spring.datasource.url=jdbc:mysql://localhost/dialer
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.jdbc.Driver


# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
#change to update for production
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


spring.thymeleaf.cache=false

directory.audios = /Users/leonardosuzan/TCC/Implementac\u0327a\u0303o/dialer/custom/
directory.lists = /Users/leonardosuzan/TCC/Implementac\u0327a\u0303o/dialer/lists/
directory.extensions = /Users/leonardosuzan/TCC/Implementac\u0327a\u0303o/dialer/asterisk
directory.ffmpeg = /Users/leonardosuzan/TCC/Implementac\u0327a\u0303o/dialer/lib/ffmpeg

multipart.maxFileSize: 20MB
multipart.maxRequestSize: 20MB

#server.address=192.168.88.249
#server.port=8080